# Assets

Shared static assets live here.

- `icons/agents/` contains provider and agent SVG icons.
- `sounds/` contains optional local notification sounds.
- `logos/` contains shared brand and service logos.
