# Notification Sounds

This directory is intentionally tracked so the Tauri bundle can include a
`assets/sounds/` resource directory after a fresh clone.

Optional notification sounds are local assets and are ignored by git. Add your
own files here after cloning the repository.
