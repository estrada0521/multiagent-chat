# /Users/okadaharuto/session 調査報告（Hub 経由）

## 全体像

| 場所 | 内容 |
|------|------|
| docs/ | AGENT.md のみ。multiagent 運用ガイド（agent-send、[Attached: …]、環境変数、multiagent context 等）。multiagent-local 側ドキュメントと同系統。 |
| logs/Claude_code_leaked/ | .agent-index.jsonl、セッション .meta、各エージェントの *.log / *.ans など。 |
| src/ | TS/TSX 約 1884 ファイル。ルートに package.json / README は見当たらず、ソース切り出し／スナップショットに近い状態。 |

.meta: セッション名 Claude_code_leaked、workspace=/Users/okadaharuto/session、agents=[codex,gemini,qwen]、定期 save による overwrite 履歴など。

.agent-index.jsonl: user/codex/gemini/qwen のメッセージ、session-topology（add-agent 等）が記録。現行 multiagent ログ形式と整合。

src の性質: main.tsx は Bun（bun:bundle feature）、Commander、React、MCP・認証・テレメトリ等を含む大規模ターミナルエージェント型 CLI（Claude Code 系）に相当。commands.ts に大量スラッシュコマンド、tasks/（LocalShell/LocalAgent/Remote/Teammate 等）、tools/、bridge・coordinator・assistant（KAIROS）など。

## multiagent-local に活かせそうな点

1. docs/AGENT.md をワークスペース直下に置く方針の継続
2. .agent-index.jsonl を正とし *.log/*.ans を残す二層ログ
3. .meta でセッション・ワークスペース・参加エージェントを JSON 保持
4. session-topology 行（topology_action / initiator 等）で UI と運用ログを一致
5. [Attached: 相対パス] を本文に埋め込み、トランスポートに attach API を増やさない設計
6. スラッシュコマンドのモジュール分割登録パターン
7. feature フラグによる dead code elimination（薄いビルド切り出し）
8. 起動時の並列 I/O（MDM・キーチェーン prefetch 等）でクリティカルパス短縮
9. history.ts の貼り付け参照・ハッシュ退避・lockfile（長文ペースト UI の参考）
10. タスク種別で実行場所（Local/Remote/In-process）を型・UI で分離
11. MCP をサービス層で厚く扱う機能分解の参考
12. upstreamproxy（CONNECT over WebSocket）は閉域・L7 制約下のトンネル設計の読み物（問題設定の参考）
13. migrations/ でユーザー設定の段階的マイグレーション
14. Ink 配下（Yoga・ANSI・選択・ハイパーリンク）は TUI 自前強化時の部品リスト
15. vim/ モジュールはキーボード中心 UX の候補
16. cost-tracker 系はトークン表示・制限 UI の要件ヒント
17. bridge/remote/daemon のフラグ分岐はローカル tmux と別マシンセッションの分岐参考
18. プラグイン・スキル初期化はセッション共通スキルディレクトリ設計の参考
19. policyLimits・managed settings は組織配布時の権限制限チェックリスト
20. 著作権のある src の無断再利用は避け、データ形式・運用・設計観点の参考に留めるのが安全

以上、実ディレクトリ一覧・.meta・jsonl・main.tsx / commands.ts / history.ts / relay.ts 等を確認した内容です。
