logs/
logs-dev/
.DS_Store
certs/
__pycache__/
.claude/settings.local.json
.gemini/settings.json
.qwen/settings.json
.qwen/settings.json.orig
.github/copilot-instructions.md

# Local / restore / IDE / E2E artifacts (do not publish)
.cursor/
.codex-restore/
.multiagent/
.playwright-mcp/

# Ephemeral external-diff snapshots (created under workspace by multiagent-chat)
.multiagent-chat-diff/

# mkcert and other local TLS material — never commit
mkcert-rootCA.pem
*.pem

# Personal site mirror / deployment tree (use your own domain separately)
site/

# Scratch, snapshots, one-off assets
memo.txt
multiagent-chat-snapshot.html
dummy_preview_test.html
gemini_*_prompt.md
SpaceX*.md
docs/multiferroics-trends*.md

# Personal or machine-specific runbooks (re-add generic variants under docs/ if needed)
docs/backblaze-b2-restic-backup.md

# Local scripts and reports
report.*.json

# Local agent / chat exports / desktop captures
docs/cursor-to-user-chat.md
*スクリーンショット*.png
ollama*.log
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Notification sounds - add your own local sound files (not tracked)
assets/sounds/*

# Local-only external access helpers (git-managed separately, do not publish)
local/external-access/

# Local external helper implementation files (kept for runtime, but not tracked here)
ops/external-access/
assets/logos/external-access.svg

# Local-only external entrypoints (not published)
bin/multiagent-cloudflare
bin/multiagent-cloudflare-watchdog
bin/multiagent-public-edge
bin/multiagent-public-edge-supervisor
