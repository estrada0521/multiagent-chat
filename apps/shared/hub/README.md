# Shared Hub Templates

Server-rendered Hub surfaces live here so concrete HTML, CSS, and browser
JavaScript are outside the backend runtime package.

The backend in `src/multiagent_chat/hub` loads these templates and injects
runtime URLs, theme tokens, and generated header assets.
