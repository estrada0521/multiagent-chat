# Reference Docs

This directory holds auxiliary material that is intentionally kept outside the main local/LAN workflow.

The primary product positioning for `multiagent-chat` is a local tmux workbench. The documents here exist for edge cases, migration notes, or optional infrastructure that should not shape the main README or onboarding flow.

## Public Access

- [public-access/README.md](public-access/README.md): index for optional external-publication notes

